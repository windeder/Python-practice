#元组方法
# t = (80,45,65,89,14,23,80,45,45)
# t2 = 1,2,5,4,6
# print(t.count(80))
# print(t.index(80))


#元组的组包和解包
# t = (80,45,65,89,14,23,80,45,45) #组包
# t2 = 1,2,5,4,6  #组包

#解包和（*）扩展解包
# a,b,c,d,e = t2
# s,*g = t2 #s接收元组第一个元素，g接收其他所有
# y,*u,i = t2 #y第一个元素，i最后一个元素，u其他所有
# print(a,b,c,d,g)

#例题 现有两个变量t=15 d=20，将两值交换数据输出到控制台
a = 15
d = 20
i,o = a,d #组包再解包赋值给i和o
print(i,o)
